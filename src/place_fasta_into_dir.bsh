#!/bin/bash

# this script will place fasta files into corresponding directories
# written by Lidia Lasecka-Dykes

path=$(pwd)
declare -a array=($(ls --ignore='*.*'))
for i in "${array[@]}"
do
#echo "$i"

var=$i

mv ${path}/${var%_L0*}*.fa $i

done
